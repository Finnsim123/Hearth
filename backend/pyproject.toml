[project]
name = "hearth"
version = "0.0.1"
description = "Self-hosted human activity recognition for Home Assistant"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.7",
    "pydantic-settings>=2.3",
    "sqlalchemy>=2.0",
    "alembic>=1.13",
    "apscheduler>=3.10",
    "influxdb-client>=1.45",
    "pandas>=2.2",
    "numpy>=1.26",
    "scikit-learn>=1.5",
    "joblib>=1.4",
    "shap>=0.46",
    "aiohttp>=3.10",          # HA WebSocket client
    "paho-mqtt>=2.1",
    "cryptography>=43.0",     # token encryption at rest (Fernet via security.py)
    "argon2-cffi>=23.1",      # password hashing (security.py only)
    "zeroconf>=0.132",        # mDNS announce for HA auto-discovery
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["hearth*"]

[project.optional-dependencies]
discovery = ["hdbscan>=0.8.38", "umap-learn>=0.5.6"]  # Phase 4; needs build tools
dev = ["pytest>=8.2", "pytest-asyncio>=0.23", "httpx>=0.27", "ruff>=0.5", "mypy>=1.10"]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["hearth/tests"]
asyncio_mode = "auto"
