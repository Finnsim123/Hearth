"""Behaviour — habits & routines analytics over the published prediction timeline
(behaviour_dashboard_design.md). Pure aggregation in summary.py; the API reads the
prediction store and hands the rows here. Descriptive, not judgmental; honest about
what's KNOWN (facts) vs INFERRED (model).
"""
